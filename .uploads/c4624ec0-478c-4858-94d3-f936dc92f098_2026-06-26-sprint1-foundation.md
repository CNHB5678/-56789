# Sprint 1: 基础框架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建项目基础框架，包括后端FastAPI+数据库+认证+文件上传+系统配置/诊断，前端Next.js+登录注册+个人空间，完成后可运行一个带用户系统的最小可用Web应用。

**Architecture:** Monorepo结构，backend/和frontend/并列。后端FastAPI+SQLAlchemy async+PostgreSQL+Redis+Celery，前端Next.js 14 App Router+TailwindCSS+shadcn/ui+Zustand。通过Docker Compose统一编排开发环境。

**Tech Stack:** Python 3.11+, FastAPI 0.104+, SQLAlchemy 2.0 (async), Alembic, Pydantic v2, python-jose, passlib[brypt], celery, redis, aioboto3, Next.js 14, React 18, TypeScript, TailwindCSS 3, shadcn/ui, Zustand

**Spec Reference:** `docs/superpowers/specs/2026-06-26-jianying-ai-agent-design.md`

---

## File Structure

```
Claw/
├── docker-compose.yml              # Docker编排（PostgreSQL/Redis/MinIO）
├── .env.example                    # 环境变量模板
├── backend/
│   ├── pyproject.toml              # 项目依赖（用uv/pip管理）
│   ├── Dockerfile
│   ├── .env.example
│   ├── alembic.ini
│   ├── alembic/
│   │   ├── env.py
│   │   └── versions/
│   └── app/
│       ├── __init__.py
│       ├── main.py                 # FastAPI应用入口
│       ├── config.py               # 配置管理（环境变量+DB配置）
│       ├── database.py             # SQLAlchemy async engine/session
│       ├── dependencies.py         # 依赖注入（get_db, get_current_user等）
│       ├── security.py             # JWT、密码哈希
│       ├── exceptions.py           # 全局异常
│       ├── models/                 # SQLAlchemy ORM模型
│       │   ├── __init__.py
│       │   ├── base.py             # Base模型
│       │   ├── user.py
│       │   ├── project.py
│       │   ├── file.py
│       │   ├── transcript.py
│       │   ├── shot.py
│       │   ├── foreground_object.py
│       │   ├── subtitle.py
│       │   ├── task_log.py
│       │   ├── wechat_binding.py
│       │   ├── diagnose_record.py
│       │   ├── system_notification.py
│       │   ├── notification_log.py
│       │   └── system_config.py
│       ├── schemas/                # Pydantic模型
│       │   ├── __init__.py
│       │   ├── common.py
│       │   ├── auth.py
│       │   ├── user.py
│       │   ├── project.py
│       │   ├── file.py
│       │   └── admin.py
│       ├── api/
│       │   ├── __init__.py
│       │   └── v1/
│       │       ├── __init__.py
│       │       ├── router.py       # 路由聚合
│       │       ├── auth.py
│       │       ├── files.py
│       │       ├── projects.py
│       │       ├── workspace.py
│       │       ├── admin_config.py
│       │       ├── admin_diagnose.py
│       │       ├── admin_notifications.py
│       │       └── ws.py
│       ├── services/
│       │   ├── __init__.py
│       │   ├── auth_service.py
│       │   ├── file_service.py
│       │   ├── oss.py
│       │   ├── sms.py
│       │   ├── config_service.py
│       │   └── diagnose_service.py
│       ├── ai/                     # AI适配层（Sprint 1仅骨架）
│       │   └── __init__.py
│       ├── pipeline/               # Pipeline（Sprint 1仅骨架）
│       │   └── __init__.py
│       ├── templates/              # 剪映草稿模板
│       │   └── draft_template/
│       └── utils/
│           ├── __init__.py
│           ├── id_generator.py
│           └── redis.py
├── frontend/
│   ├── package.json
│   ├── tsconfig.json
│   ├── next.config.js
│   ├── tailwind.config.js
│   ├── postcss.config.js
│   ├── Dockerfile
│   ├── .env.example
│   └── src/
│       ├── app/
│       │   ├── layout.tsx
│       │   ├── page.tsx
│       │   ├── globals.css
│       │   ├── login/page.tsx
│       │   ├── register/page.tsx
│       │   ├── forgot-password/page.tsx
│       │   └── workspace/
│       │       ├── layout.tsx
│       │       └── page.tsx
│       ├── components/
│       │   ├── ui/                 # shadcn/ui组件
│       │   ├── layout/
│       │   │   ├── Header.tsx
│       │   │   └── AuthGuard.tsx
│       │   ├── auth/
│       │   │   ├── LoginForm.tsx
│       │   │   ├── RegisterForm.tsx
│       │   │   └── ForgotPasswordForm.tsx
│       │   └── workspace/
│       │       ├── ProjectCard.tsx
│       │       ├── ProjectList.tsx
│       │       └── StatsBar.tsx
│       ├── lib/
│       │   ├── api.ts
│       │   ├── auth.ts
│       │   └── utils.ts
│       ├── hooks/
│       │   └── useAuth.ts
│       └── store/
│           └── useAuthStore.ts
└── tests/
    └── backend/
        ├── conftest.py
        ├── test_auth.py
        ├── test_config.py
        └── test_diagnose.py
```

---

## Phase 1: 项目初始化与开发环境

### Task 1: 初始化项目结构与Docker开发环境

**Files:**
- Create: `docker-compose.yml`
- Create: `.env.example`
- Create: `backend/.env.example`
- Create: `frontend/.env.example`

- [ ] **Step 1: 创建根目录docker-compose.yml**

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: jianying-postgres
    environment:
      POSTGRES_USER: jianying
      POSTGRES_PASSWORD: jianying123
      POSTGRES_DB: jianying
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jianying"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: jianying-redis
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: jianying-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 3

volumes:
  pgdata:
  redisdata:
  miniodata:
```

- [ ] **Step 2: 创建根目录.env.example**

```env
# Environment
ENVIRONMENT=development
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Database
DATABASE_URL=postgresql+asyncpg://jianying:jianying123@localhost:5432/jianying
DATABASE_URL_SYNC=postgresql://jianying:jianying123@localhost:5432/jianying

# Redis
REDIS_URL=redis://localhost:6379/0

# MinIO (开发环境)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=jianying
MINIO_SECURE=false
```

- [ ] **Step 3: 创建backend/.env.example**（同根目录.env.example，供后端单独使用）

```env
ENVIRONMENT=development
SECRET_KEY=dev-secret-key-change-in-production
DATABASE_URL=postgresql+asyncpg://jianying:jianying123@localhost:5432/jianying
DATABASE_URL_SYNC=postgresql://jianying:jianying123@localhost:5432/jianying
REDIS_URL=redis://localhost:6379/0
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=jianying
MINIO_SECURE=false
ACCESS_TOKEN_EXPIRE_MINUTES=120
REFRESH_TOKEN_EXPIRE_DAYS=7
```

- [ ] **Step 4: 创建frontend/.env.example**

```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws
```

- [ ] **Step 5: 启动Docker开发环境**

Run:
```powershell
cd 'e:\开发工程\Claw'
docker-compose up -d
```

Expected: 三个容器启动成功（postgres, redis, minio）

Run:
```powershell
docker-compose ps
```

Expected: 三个服务状态均为Up (healthy)

- [ ] **Step 6: 创建MinIO bucket**

Run:
```powershell
# Wait for MinIO to be healthy, then create bucket via mc or API
docker exec jianying-minio mc alias set local http://localhost:9000 minioadmin minioadmin123
docker exec jianying-minio mc mb local/jianying
```

Expected: `Bucket created successfully: local/jianying`

- [ ] **Step 7: Commit**

```bash
git add docker-compose.yml .env.example backend/.env.example frontend/.env.example
git commit -m "chore: initialize project structure with docker-compose"
```

---

### Task 2: 后端FastAPI基础框架

**Files:**
- Create: `backend/pyproject.toml`
- Create: `backend/app/__init__.py`
- Create: `backend/app/main.py`
- Create: `backend/app/config.py`
- Create: `backend/app/database.py`
- Create: `backend/app/exceptions.py`
- Test: `tests/backend/conftest.py`

- [ ] **Step 1: 创建backend/pyproject.toml**

```toml
[project]
name = "jianying-ai-agent"
version = "0.1.0"
description = "AI Agent for generating JianYing draft files"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "sqlalchemy[asyncio]>=2.0.23",
    "asyncpg>=0.29.0",
    "psycopg2-binary>=2.9.9",
    "alembic>=1.12.0",
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
    "python-jose[cryptography]>=3.3.0",
    "passlib[bcrypt]>=1.7.4",
    "python-multipart>=0.0.6",
    "celery[redis]>=5.3.4",
    "redis>=5.0.1",
    "aioboto3>=12.0.0",
    "boto3>=1.34.0",
    "httpx>=0.25.0",
    "wechatpy>=2.1.0",
    "python-dotenv>=1.0.0",
    "aiofiles>=23.2.0",
    "Pillow>=10.1.0",
    "cryptography>=41.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "httpx>=0.25.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
```

- [ ] **Step 2: 创建backend/app/__init__.py**（空文件）

- [ ] **Step 3: 创建backend/app/config.py**

```python
from functools import lru_cache
from typing import Optional
from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    ENVIRONMENT: str = "development"
    SECRET_KEY: str = "dev-secret-key"
    DEBUG: bool = True

    # Database
    DATABASE_URL: str = "postgresql+asyncpg://jianying:jianying123@localhost:5432/jianying"
    DATABASE_URL_SYNC: str = "postgresql://jianying:jianying123@localhost:5432/jianying"

    # Redis
    REDIS_URL: str = "redis://localhost:6379/0"

    # JWT
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 120
    REFRESH_TOKEN_EXPIRE_DAYS: int = 7
    JWT_ALGORITHM: str = "HS256"

    # MinIO / OSS
    MINIO_ENDPOINT: str = "localhost:9000"
    MINIO_ACCESS_KEY: str = "minioadmin"
    MINIO_SECRET_KEY: str = "minioadmin123"
    MINIO_BUCKET: str = "jianying"
    MINIO_SECURE: bool = False

    # Upload limits
    MAX_AUDIO_SIZE_MB: int = 100
    MAX_IMAGE_SIZE_MB: int = 20

    # CORS
    CORS_ORIGINS: list[str] = ["http://localhost:3000", "http://127.0.0.1:3000"]

    model_config = {"env_file": ".env", "extra": "ignore"}


@lru_cache()
def get_settings() -> Settings:
    return Settings()
```

- [ ] **Step 4: 创建backend/app/database.py**

```python
from sqlalchemy.ext.asyncio import AsyncSession, create_async_engine, async_sessionmaker
from sqlalchemy.orm import DeclarativeBase
from app.config import get_settings

settings = get_settings()

engine = create_async_engine(
    settings.DATABASE_URL,
    echo=settings.DEBUG,
    pool_pre_ping=True,
    pool_size=10,
    max_overflow=20,
)

AsyncSessionLocal = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)


class Base(DeclarativeBase):
    pass


async def get_db():
    async with AsyncSessionLocal() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
        finally:
            await session.close()
```

- [ ] **Step 5: 创建backend/app/exceptions.py**

```python
from fastapi import HTTPException, Request
from fastapi.responses import JSONResponse


class AppException(Exception):
    def __init__(self, code: int, message: str, status_code: int = 400):
        self.code = code
        self.message = message
        self.status_code = status_code


async def app_exception_handler(request: Request, exc: AppException):
    return JSONResponse(
        status_code=exc.status_code,
        content={"code": exc.code, "message": exc.message, "data": None},
    )


async def http_exception_handler(request: Request, exc: HTTPException):
    return JSONResponse(
        status_code=exc.status_code,
        content={"code": exc.status_code, "message": exc.detail, "data": None},
    )


async def general_exception_handler(request: Request, exc: Exception):
    return JSONResponse(
        status_code=500,
        content={"code": 500, "message": "服务器内部错误", "data": None},
    )
```

- [ ] **Step 6: 创建backend/app/main.py**

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.config import get_settings
from app.exceptions import AppException, app_exception_handler, http_exception_handler, general_exception_handler
from app.api.v1.router import api_router

settings = get_settings()

app = FastAPI(
    title="剪映AI草稿生成Agent",
    description="AI Agent for generating JianYing draft files from audio/text",
    version="0.1.0",
    docs_url="/docs" if settings.ENVIRONMENT == "development" else None,
    redoc_url="/redoc" if settings.ENVIRONMENT == "development" else None,
)

# CORS
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.CORS_ORIGINS,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# Exception handlers
app.add_exception_handler(AppException, app_exception_handler)
app.add_exception_handler(404, http_exception_handler)
app.add_exception_handler(Exception, general_exception_handler)

# Routes
app.include_router(api_router, prefix="/api/v1")


@app.get("/api/health")
async def health_check():
    return {"status": "ok", "version": "0.1.0"}
```

- [ ] **Step 7: 创建基础API路由**

Create `backend/app/api/__init__.py` (empty)
Create `backend/app/api/v1/__init__.py` (empty)
Create `backend/app/api/v1/router.py`:

```python
from fastapi import APIRouter
from app.api.v1 import auth, files, projects, workspace, admin_config, admin_diagnose, admin_notifications

api_router = APIRouter()
api_router.include_router(auth.router, prefix="/auth", tags=["认证"])
api_router.include_router(files.router, prefix="/files", tags=["文件"])
api_router.include_router(projects.router, prefix="/projects", tags=["项目"])
api_router.include_router(workspace.router, prefix="/workspace", tags=["个人空间"])
api_router.include_router(admin_config.router, prefix="/admin/config", tags=["管理员-配置"])
api_router.include_router(admin_diagnose.router, prefix="/admin/diagnose", tags=["管理员-诊断"])
api_router.include_router(admin_notifications.router, prefix="/admin/notifications", tags=["管理员-通知"])
```

- [ ] **Step 8: 创建空的路由占位文件**（避免导入错误）

For each of the following, create a minimal router file:

`backend/app/api/v1/auth.py`:
```python
from fastapi import APIRouter
router = APIRouter()
```

Create similar empty routers for: files.py, projects.py, workspace.py, admin_config.py, admin_diagnose.py, admin_notifications.py

- [ ] **Step 9: 安装后端依赖并启动验证**

Run:
```powershell
cd 'e:\开发工程\Claw\backend'
pip install -e ".[dev]" --break-system-packages
```

Expected: 依赖安装成功

Run:
```powershell
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

In a new terminal, run:
```powershell
curl http://localhost:8000/api/health
```

Expected: `{"status":"ok","version":"0.1.0"}`

Run:
```powershell
curl http://localhost:8000/docs
```

Expected: Swagger文档页面可访问

Stop the server after verification.

- [ ] **Step 10: 创建测试配置和conftest**

Create `tests/backend/conftest.py`:
```python
import asyncio
import pytest
from httpx import AsyncClient, ASGITransport
from app.main import app
from app.database import Base, engine, AsyncSessionLocal


@pytest.fixture(scope="session")
def event_loop():
    loop = asyncio.get_event_loop_policy().new_event_loop()
    yield loop
    loop.close()


@pytest.fixture(autouse=True)
async def setup_db():
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    yield
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.drop_all)


@pytest.fixture
async def client():
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as ac:
        yield ac
```

- [ ] **Step 11: 运行基础测试验证框架**

Create `tests/backend/test_health.py`:
```python
import pytest

@pytest.mark.asyncio
async def test_health_check(client):
    response = await client.get("/api/health")
    assert response.status_code == 200
    data = response.json()
    assert data["status"] == "ok"
```

Run:
```powershell
cd 'e:\开发工程\Claw\backend'
pytest tests/../tests/backend/test_health.py -v
```

Expected: 1 test passed

- [ ] **Step 12: Commit**

```bash
git add backend/pyproject.toml backend/app/ tests/backend/conftest.py tests/backend/test_health.py
git commit -m "feat: initialize FastAPI backend framework"
```

---

## Phase 2: 数据库模型与迁移

### Task 3: SQLAlchemy ORM模型

**Files:**
- Create: `backend/app/models/base.py`
- Create: `backend/app/models/__init__.py`
- Create: `backend/app/models/user.py`
- Create: `backend/app/models/project.py`
- Create: `backend/app/models/file.py`
- Create: `backend/app/models/transcript.py`
- Create: `backend/app/models/shot.py`
- Create: `backend/app/models/foreground_object.py`
- Create: `backend/app/models/subtitle.py`
- Create: `backend/app/models/task_log.py`
- Create: `backend/app/models/wechat_binding.py`
- Create: `backend/app/models/diagnose_record.py`
- Create: `backend/app/models/system_notification.py`
- Create: `backend/app/models/notification_log.py`
- Create: `backend/app/models/system_config.py`
- Modify: `backend/app/database.py`

- [ ] **Step 1: 创建models/base.py**（添加通用字段mixin）

```python
import uuid
from datetime import datetime
from sqlalchemy import DateTime, func
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base


class UUIDPrimaryKeyMixin:
    id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), primary_key=True, default=uuid.uuid4
    )


class TimestampMixin:
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), nullable=False
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now(), nullable=False
    )
```

- [ ] **Step 2: 创建models/user.py**

```python
import uuid
from datetime import datetime, date
from sqlalchemy import String, Boolean, Integer, Date, DateTime, func, ForeignKey
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin, TimestampMixin


class User(Base, UUIDPrimaryKeyMixin, TimestampMixin):
    __tablename__ = "users"

    phone: Mapped[str] = mapped_column(String(20), unique=True, index=True, nullable=False)
    password_hash: Mapped[str] = mapped_column(String(255), nullable=False)
    nickname: Mapped[str] = mapped_column(String(50), default="用户")
    avatar_url: Mapped[str | None] = mapped_column(String(500), nullable=True)
    wechat_openid: Mapped[str | None] = mapped_column(String(100), unique=True, nullable=True)
    wechat_unionid: Mapped[str | None] = mapped_column(String(100), nullable=True)
    quota_monthly: Mapped[int] = mapped_column(Integer, default=50)
    quota_used: Mapped[int] = mapped_column(Integer, default=0)
    quota_reset_date: Mapped[date | None] = mapped_column(Date, nullable=True)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    is_admin: Mapped[bool] = mapped_column(Boolean, default=False)

    projects = relationship("Project", back_populates="user", cascade="all, delete-orphan")
```

- [ ] **Step 3: 创建models/project.py**

```python
import uuid
from sqlalchemy import String, Boolean, Integer, BigInteger, Float, ForeignKey, Text, JSON
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin, TimestampMixin


class Project(Base, UUIDPrimaryKeyMixin, TimestampMixin):
    __tablename__ = "projects"

    user_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)
    title: Mapped[str] = mapped_column(String(200), nullable=False)
    mode: Mapped[str] = mapped_column(String(20), default="audio")
    status: Mapped[str] = mapped_column(String(30), default="queued")
    current_stage: Mapped[int] = mapped_column(Integer, default=0)
    progress: Mapped[int] = mapped_column(Integer, default=0)
    progress_message: Mapped[str | None] = mapped_column(Text, nullable=True)
    duration_us: Mapped[int] = mapped_column(BigInteger, default=0)
    canvas_width: Mapped[int] = mapped_column(Integer, default=1708)
    canvas_height: Mapped[int] = mapped_column(Integer, default=960)
    subtitle_enabled: Mapped[bool] = mapped_column(Boolean, default=True)
    subtitle_style: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    bgm_file_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("files.id"), nullable=True)
    original_audio_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("files.id"), nullable=True)
    draft_zip_path: Mapped[str | None] = mapped_column(String(500), nullable=True)
    preview_video_path: Mapped[str | None] = mapped_column(String(500), nullable=True)
    export_video_path: Mapped[str | None] = mapped_column(String(500), nullable=True)
    cover_image_path: Mapped[str | None] = mapped_column(String(500), nullable=True)
    error_message: Mapped[str | None] = mapped_column(Text, nullable=True)
    celery_task_id: Mapped[str | None] = mapped_column(String(100), nullable=True)
    is_deleted: Mapped[bool] = mapped_column(Boolean, default=False)

    user = relationship("User", back_populates="projects")
    shots = relationship("Shot", back_populates="project", cascade="all, delete-orphan", order_by="Shot.index")
    transcript = relationship("Transcript", back_populates="project", uselist=False, cascade="all, delete-orphan")
    subtitles = relationship("Subtitle", back_populates="project", cascade="all, delete-orphan", order_by="Subtitle.index")
```

- [ ] **Step 4: 创建models/file.py**

```python
import uuid
from sqlalchemy import String, BigInteger, Integer, ForeignKey, JSON
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin, TimestampMixin


class File(Base, UUIDPrimaryKeyMixin, TimestampMixin):
    __tablename__ = "files"

    user_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=True)
    project_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("projects.id"), nullable=True)
    file_type: Mapped[str] = mapped_column(String(30), nullable=False)
    source: Mapped[str] = mapped_column(String(30), default="upload")
    filename: Mapped[str] = mapped_column(String(255), nullable=False)
    oss_key: Mapped[str] = mapped_column(String(500), nullable=False)
    mime_type: Mapped[str] = mapped_column(String(100), default="application/octet-stream")
    file_size: Mapped[int] = mapped_column(BigInteger, default=0)
    width: Mapped[int | None] = mapped_column(Integer, nullable=True)
    height: Mapped[int | None] = mapped_column(Integer, nullable=True)
    duration_us: Mapped[int | None] = mapped_column(BigInteger, nullable=True)
    metadata: Mapped[dict | None] = mapped_column(JSON, nullable=True)
```

- [ ] **Step 5: 创建models/transcript.py**

```python
import uuid
from sqlalchemy import String, Boolean, Text, ForeignKey, JSON
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin, TimestampMixin


class Transcript(Base, UUIDPrimaryKeyMixin, TimestampMixin):
    __tablename__ = "transcripts"

    project_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("projects.id"), nullable=False, unique=True)
    content: Mapped[str] = mapped_column(Text, default="")
    original_content: Mapped[str] = mapped_column(Text, default="")
    words: Mapped[list | None] = mapped_column(JSON, nullable=True)
    is_confirmed: Mapped[bool] = mapped_column(Boolean, default=False)
    tts_voice_id: Mapped[str | None] = mapped_column(String(50), nullable=True)

    project = relationship("Project", back_populates="transcript")
```

- [ ] **Step 6: 创建models/shot.py**

```python
import uuid
from sqlalchemy import String, Integer, BigInteger, Float, Text, ForeignKey, JSON
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin


class Shot(Base, UUIDPrimaryKeyMixin):
    __tablename__ = "shots"

    project_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("projects.id"), nullable=False)
    index: Mapped[int] = mapped_column(Integer, nullable=False)
    start_us: Mapped[int] = mapped_column(BigInteger, default=0)
    end_us: Mapped[int] = mapped_column(BigInteger, default=0)
    text: Mapped[str] = mapped_column(Text, default="")
    scene_description: Mapped[str | None] = mapped_column(Text, nullable=True)
    keywords: Mapped[list | None] = mapped_column(JSON, nullable=True)
    emotion: Mapped[str | None] = mapped_column(String(20), nullable=True)
    bg_image_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("files.id"), nullable=True)
    bg_prompt: Mapped[str | None] = mapped_column(Text, nullable=True)
    bg_scale: Mapped[float] = mapped_column(Float, default=1.0)
    bg_transform_x: Mapped[float] = mapped_column(Float, default=0.0)
    bg_transform_y: Mapped[float] = mapped_column(Float, default=0.0)

    project = relationship("Project", back_populates="shots")
    foreground_objects = relationship("ForegroundObject", back_populates="shot", cascade="all, delete-orphan")
```

- [ ] **Step 7: 创建models/foreground_object.py**

```python
import uuid
from sqlalchemy import String, Float, BigInteger, Integer, Text, ForeignKey
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin, TimestampMixin


class ForegroundObject(Base, UUIDPrimaryKeyMixin, TimestampMixin):
    __tablename__ = "foreground_objects"

    shot_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("shots.id"), nullable=False)
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    image_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("files.id"), nullable=True)
    matting_image_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("files.id"), nullable=True)
    prompt: Mapped[str | None] = mapped_column(Text, nullable=True)
    scale: Mapped[float] = mapped_column(Float, default=1.0)
    position_x: Mapped[float] = mapped_column(Float, default=0.0)
    position_y: Mapped[float] = mapped_column(Float, default=0.0)
    rotation: Mapped[float] = mapped_column(Float, default=0.0)
    appear_time_us: Mapped[int] = mapped_column(BigInteger, default=0)
    disappear_time_us: Mapped[int] = mapped_column(BigInteger, default=0)
    animation_type: Mapped[str | None] = mapped_column(String(50), nullable=True)
    animation_duration_us: Mapped[int] = mapped_column(BigInteger, default=500000)
    z_index: Mapped[int] = mapped_column(Integer, default=1)

    shot = relationship("Shot", back_populates="foreground_objects")
```

- [ ] **Step 8: 创建models/subtitle.py**

```python
import uuid
from sqlalchemy import Integer, BigInteger, Text, ForeignKey, JSON
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin


class Subtitle(Base, UUIDPrimaryKeyMixin):
    __tablename__ = "subtitles"

    project_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("projects.id"), nullable=False)
    index: Mapped[int] = mapped_column(Integer, nullable=False)
    start_us: Mapped[int] = mapped_column(BigInteger, default=0)
    end_us: Mapped[int] = mapped_column(BigInteger, default=0)
    text: Mapped[str] = mapped_column(Text, default="")
    style: Mapped[dict | None] = mapped_column(JSON, nullable=True)

    project = relationship("Project", back_populates="subtitles")
```

- [ ] **Step 9: 创建models/task_log.py**

```python
import uuid
from datetime import datetime
from sqlalchemy import String, Integer, Text, ForeignKey, DateTime
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin


class CeleryTaskLog(Base, UUIDPrimaryKeyMixin):
    __tablename__ = "celery_task_logs"

    project_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("projects.id"), nullable=True)
    stage: Mapped[int] = mapped_column(Integer, default=0)
    stage_name: Mapped[str] = mapped_column(String(50), default="")
    status: Mapped[str] = mapped_column(String(20), default="pending")
    progress: Mapped[int] = mapped_column(Integer, default=0)
    message: Mapped[str | None] = mapped_column(Text, nullable=True)
    error_detail: Mapped[str | None] = mapped_column(Text, nullable=True)
    started_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    finished_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
```

- [ ] **Step 10: 创建models/wechat_binding.py**

```python
import uuid
from datetime import datetime
from sqlalchemy import String, Boolean, DateTime, ForeignKey
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin


class WechatBinding(Base, UUIDPrimaryKeyMixin):
    __tablename__ = "wechat_bindings"

    user_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False, unique=True)
    openid: Mapped[str] = mapped_column(String(100), unique=True, nullable=False, index=True)
    unionid: Mapped[str | None] = mapped_column(String(100), nullable=True)
    nickname: Mapped[str | None] = mapped_column(String(100), nullable=True)
    avatar_url: Mapped[str | None] = mapped_column(String(500), nullable=True)
    subscribed: Mapped[bool] = mapped_column(Boolean, default=False)
    bound_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
```

- [ ] **Step 11: 创建models/diagnose_record.py**

```python
import uuid
from datetime import datetime
from sqlalchemy import String, Integer, DateTime, JSON
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin


class DiagnoseRecord(Base, UUIDPrimaryKeyMixin):
    __tablename__ = "diagnose_records"

    started_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    finished_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    status: Mapped[str] = mapped_column(String(20), default="running")
    total_checks: Mapped[int] = mapped_column(Integer, default=0)
    passed: Mapped[int] = mapped_column(Integer, default=0)
    warnings: Mapped[int] = mapped_column(Integer, default=0)
    failed: Mapped[int] = mapped_column(Integer, default=0)
    results: Mapped[list | None] = mapped_column(JSON, nullable=True)
    triggered_by: Mapped[str] = mapped_column(String(50), default="manual")
```

- [ ] **Step 12: 创建models/system_notification.py**

```python
import uuid
from datetime import datetime
from sqlalchemy import String, Integer, Text, DateTime, ForeignKey, JSON
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin, TimestampMixin


class SystemNotification(Base, UUIDPrimaryKeyMixin, TimestampMixin):
    __tablename__ = "system_notifications"

    type: Mapped[str] = mapped_column(String(30), nullable=False)
    title: Mapped[str] = mapped_column(String(200), nullable=False)
    content: Mapped[str] = mapped_column(Text, nullable=False)
    status: Mapped[str] = mapped_column(String(20), default="draft")
    send_scope: Mapped[str] = mapped_column(String(20), default="wechat_bound")
    total_recipients: Mapped[int] = mapped_column(Integer, default=0)
    sent_count: Mapped[int] = mapped_column(Integer, default=0)
    failed_count: Mapped[int] = mapped_column(Integer, default=0)
    trigger_source: Mapped[str | None] = mapped_column(String(50), nullable=True)
    related_diagnose_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), nullable=True)
    scheduled_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    sent_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    created_by: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=True)
```

- [ ] **Step 13: 创建models/notification_log.py**

```python
import uuid
from datetime import datetime
from sqlalchemy import String, Text, DateTime, ForeignKey
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin


class NotificationLog(Base, UUIDPrimaryKeyMixin):
    __tablename__ = "notification_logs"

    notification_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("system_notifications.id"), nullable=False)
    user_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=False)
    channel: Mapped[str] = mapped_column(String(20), default="wechat")
    status: Mapped[str] = mapped_column(String(20), default="pending")
    wechat_msgid: Mapped[str | None] = mapped_column(String(100), nullable=True)
    error_message: Mapped[str | None] = mapped_column(Text, nullable=True)
    sent_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
```

- [ ] **Step 14: 创建models/system_config.py**

```python
from datetime import datetime
from sqlalchemy import String, Boolean, Text, DateTime, func, UniqueConstraint
from app.database import Base
from app.models.base import UUIDPrimaryKeyMixin
from sqlalchemy.orm import Mapped, mapped_column


class SystemConfig(Base, UUIDPrimaryKeyMixin):
    __tablename__ = "system_configs"
    __table_args__ = (UniqueConstraint("category", "key", name="uq_category_key"),)

    category: Mapped[str] = mapped_column(String(50), nullable=False, index=True)
    key: Mapped[str] = mapped_column(String(100), nullable=False)
    value: Mapped[str] = mapped_column(Text, default="")
    value_type: Mapped[str] = mapped_column(String(20), default="string")
    description: Mapped[str | None] = mapped_column(String(500), nullable=True)
    is_secret: Mapped[bool] = mapped_column(Boolean, default=False)
    is_enabled: Mapped[bool] = mapped_column(Boolean, default=True)
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
```

- [ ] **Step 15: 创建models/__init__.py（统一导出所有模型）**

```python
from app.models.user import User
from app.models.project import Project
from app.models.file import File
from app.models.transcript import Transcript
from app.models.shot import Shot
from app.models.foreground_object import ForegroundObject
from app.models.subtitle import Subtitle
from app.models.task_log import CeleryTaskLog
from app.models.wechat_binding import WechatBinding
from app.models.diagnose_record import DiagnoseRecord
from app.models.system_notification import SystemNotification
from app.models.notification_log import NotificationLog
from app.models.system_config import SystemConfig

__all__ = [
    "User", "Project", "File", "Transcript", "Shot",
    "ForegroundObject", "Subtitle", "CeleryTaskLog",
    "WechatBinding", "DiagnoseRecord", "SystemNotification",
    "NotificationLog", "SystemConfig",
]
```

- [ ] **Step 16: Commit**

```bash
git add backend/app/models/
git commit -m "feat: add all SQLAlchemy ORM models"
```

---

### Task 4: Alembic数据库迁移配置

**Files:**
- Create: `backend/alembic.ini`
- Create: `backend/alembic/env.py`
- Create: `backend/alembic/versions/` directory
- Test: 运行migration创建所有表

- [ ] **Step 1: 初始化Alembic**

Run:
```powershell
cd 'e:\开发工程\Claw\backend'
alembic init alembic
```

Expected: alembic/ directory and alembic.ini created

- [ ] **Step 2: 修改alembic.ini中的数据库连接**

Edit `backend/alembic.ini`, change sqlalchemy.url to:
```ini
sqlalchemy.url = postgresql://jianying:jianying123@localhost:5432/jianying
```

- [ ] **Step 3: 修改alembic/env.py以使用async和模型元数据**

Replace the content of `backend/alembic/env.py` with:

```python
import asyncio
from logging.config import fileConfig
from sqlalchemy import pool
from sqlalchemy.engine import Connection
from sqlalchemy.ext.asyncio import async_engine_from_config
from alembic import context
import sys
import os
sys.path.insert(0, os.path.dirname(os.path.dirname(__file__)))

from app.database import Base
from app.config import get_settings
import app.models  # noqa: F401 - import all models

config = context.config
settings = get_settings()

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

config.set_main_option("sqlalchemy.url", settings.DATABASE_URL)
target_metadata = Base.metadata


def run_migrations_offline() -> None:
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url,
        target_metadata=target_metadata,
        literal_binds=True,
        dialect_opts={"paramstyle": "named"},
    )
    with context.begin_transaction():
        context.run_migrations()


def do_run_migrations(connection: Connection) -> None:
    context.configure(connection=connection, target_metadata=target_metadata)
    with context.begin_transaction():
        context.run_migrations()


async def run_async_migrations() -> None:
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)
    await connectable.dispose()


def run_migrations_online() -> None:
    asyncio.run(run_async_migrations())


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

- [ ] **Step 4: 生成并运行初始迁移**

Run:
```powershell
cd 'e:\开发工程\Claw\backend'
alembic revision --autogenerate -m "initial tables"
```

Expected: A new migration file created in alembic/versions/

Run:
```powershell
alembic upgrade head
```

Expected: All tables created in PostgreSQL

Run:
```powershell
docker exec jianying-postgres psql -U jianying -d jianying -c "\dt"
```

Expected: All 13 tables listed (users, projects, files, transcripts, shots, foreground_objects, subtitles, celery_task_logs, wechat_bindings, diagnose_records, system_notifications, notification_logs, system_configs)

- [ ] **Step 5: Commit**

```bash
git add backend/alembic.ini backend/alembic/
git commit -m "feat: configure alembic and create initial migration"
```

---

## Phase 3: 用户认证系统

### Task 5: 安全工具（JWT、密码哈希、ID生成）

**Files:**
- Create: `backend/app/security.py`
- Create: `backend/app/utils/id_generator.py`
- Create: `backend/app/utils/__init__.py`
- Create: `backend/app/utils/redis.py`

- [ ] **Step 1: 创建backend/app/utils/__init__.py**（空文件）

- [ ] **Step 2: 创建backend/app/utils/id_generator.py**

```python
import uuid
import secrets
import string


def generate_uuid() -> uuid.UUID:
    return uuid.uuid4()


def generate_short_id(length: int = 8) -> str:
    alphabet = string.ascii_letters + string.digits
    return "".join(secrets.choice(alphabet) for _ in range(length))


def generate_verification_code(length: int = 6) -> str:
    return "".join(secrets.choice(string.digits) for _ in range(length))
```

- [ ] **Step 3: 创建backend/app/utils/redis.py**

```python
import redis.asyncio as aioredis
from app.config import get_settings

settings = get_settings()
_redis_client: aioredis.Redis | None = None


async def get_redis() -> aioredis.Redis:
    global _redis_client
    if _redis_client is None:
        _redis_client = aioredis.from_url(settings.REDIS_URL, decode_responses=True)
    return _redis_client
```

- [ ] **Step 4: 创建backend/app/security.py**

```python
from datetime import datetime, timedelta, timezone
from typing import Any
from jose import jwt, JWTError
from passlib.context import CryptContext
from app.config import get_settings

settings = get_settings()
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")


def hash_password(password: str) -> str:
    return pwd_context.hash(password)


def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)


def create_access_token(subject: str, expires_delta: timedelta | None = None) -> str:
    if expires_delta:
        expire = datetime.now(timezone.utc) + expires_delta
    else:
        expire = datetime.now(timezone.utc) + timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode = {"exp": expire, "sub": str(subject), "type": "access"}
    return jwt.encode(to_encode, settings.SECRET_KEY, algorithm=settings.JWT_ALGORITHM)


def create_refresh_token(subject: str) -> str:
    expire = datetime.now(timezone.utc) + timedelta(days=settings.REFRESH_TOKEN_EXPIRE_DAYS)
    to_encode = {"exp": expire, "sub": str(subject), "type": "refresh"}
    return jwt.encode(to_encode, settings.SECRET_KEY, algorithm=settings.JWT_ALGORITHM)


def decode_token(token: str) -> dict[str, Any]:
    try:
        payload = jwt.decode(token, settings.SECRET_KEY, algorithms=[settings.JWT_ALGORITHM])
        return payload
    except JWTError:
        raise ValueError("Invalid token")


def encrypt_secret(value: str) -> str:
    """简单AES加密（用于系统配置中的密钥字段）"""
    from cryptography.fernet import Fernet
    import base64
    key = base64.urlsafe_b64encode(settings.SECRET_KEY.encode().ljust(32)[:32])
    f = Fernet(key)
    return f.encrypt(value.encode()).decode()


def decrypt_secret(encrypted_value: str) -> str:
    from cryptography.fernet import Fernet
    import base64
    key = base64.urlsafe_b64encode(settings.SECRET_KEY.encode().ljust(32)[:32])
    f = Fernet(key)
    return f.decrypt(encrypted_value.encode()).decode()


def mask_secret(value: str, show_chars: int = 2) -> str:
    if not value or len(value) <= show_chars * 2:
        return "***"
    return value[:show_chars] + "***" + value[-show_chars:]
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/security.py backend/app/utils/
git commit -m "feat: add security utilities (JWT, password hashing, encryption)"
```

---

### Task 6: Pydantic Schemas和依赖注入

**Files:**
- Create: `backend/app/schemas/common.py`
- Create: `backend/app/schemas/auth.py`
- Create: `backend/app/schemas/user.py`
- Create: `backend/app/schemas/__init__.py`
- Create: `backend/app/dependencies.py`
- Test: `tests/backend/test_auth.py` (partial)

- [ ] **Step 1: 创建backend/app/schemas/__init__.py**（空文件）

- [ ] **Step 2: 创建backend/app/schemas/common.py**

```python
from typing import Generic, TypeVar, Optional, List
from pydantic import BaseModel

T = TypeVar("T")


class ResponseModel(BaseModel, Generic[T]):
    code: int = 0
    message: str = "success"
    data: Optional[T] = None


class PageParams(BaseModel):
    page: int = 1
    page_size: int = 20


class PageResponse(BaseModel, Generic[T]):
    items: List[T]
    total: int
    page: int
    page_size: int
```

- [ ] **Step 3: 创建backend/app/schemas/auth.py**

```python
from pydantic import BaseModel, Field
from uuid import UUID
from datetime import datetime


class SendCodeRequest(BaseModel):
    phone: str = Field(..., pattern=r"^1[3-9]\d{9}$")


class LoginByPhoneRequest(BaseModel):
    phone: str = Field(..., pattern=r"^1[3-9]\d{9}$")
    code: str = Field(..., min_length=4, max_length=8)


class LoginByPasswordRequest(BaseModel):
    phone: str = Field(..., pattern=r"^1[3-9]\d{9}$")
    password: str = Field(..., min_length=6, max_length=50)


class RegisterRequest(BaseModel):
    phone: str = Field(..., pattern=r"^1[3-9]\d{9}$")
    code: str = Field(..., min_length=4, max_length=8)
    password: str = Field(..., min_length=6, max_length=50)


class ForgotPasswordRequest(BaseModel):
    phone: str = Field(..., pattern=r"^1[3-9]\d{9}$")
    code: str = Field(..., min_length=4, max_length=8)
    new_password: str = Field(..., min_length=6, max_length=50)


class ChangePasswordRequest(BaseModel):
    old_password: str
    new_password: str = Field(..., min_length=6, max_length=50)


class TokenResponse(BaseModel):
    access_token: str
    refresh_token: str
    token_type: str = "bearer"
    expires_in: int


class UserInfoResponse(BaseModel):
    id: UUID
    phone: str
    nickname: str
    avatar_url: str | None
    quota_monthly: int
    quota_used: int
    wechat_bound: bool
    created_at: datetime

    model_config = {"from_attributes": True}
```

- [ ] **Step 4: 创建backend/app/schemas/user.py**

```python
from pydantic import BaseModel
from uuid import UUID


class UpdateProfileRequest(BaseModel):
    nickname: str | None = None
    avatar_url: str | None = None
```

- [ ] **Step 5: 创建backend/app/dependencies.py**

```python
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from jose import JWTError
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from app.database import get_db
from app.security import decode_token
from app.models.user import User

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login-password", auto_error=False)


async def get_current_user(
    token: str | None = Depends(oauth2_scheme),
    db: AsyncSession = Depends(get_db),
) -> User:
    if not token:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="未提供认证凭证",
            headers={"WWW-Authenticate": "Bearer"},
        )
    try:
        payload = decode_token(token)
        user_id = payload.get("sub")
        if payload.get("type") != "access":
            raise HTTPException(status_code=401, detail="Token类型错误")
        if user_id is None:
            raise HTTPException(status_code=401, detail="无效的Token")
    except (JWTError, ValueError):
        raise HTTPException(status_code=401, detail="无效的Token")

    result = await db.execute(select(User).where(User.id == user_id))
    user = result.scalar_one_or_none()
    if user is None:
        raise HTTPException(status_code=401, detail="用户不存在")
    if not user.is_active:
        raise HTTPException(status_code=403, detail="账号已被禁用")
    return user


async def get_current_admin(user: User = Depends(get_current_user)) -> User:
    if not user.is_admin:
        raise HTTPException(status_code=403, detail="需要管理员权限")
    return user


async def get_optional_user(
    token: str | None = Depends(oauth2_scheme),
    db: AsyncSession = Depends(get_db),
) -> User | None:
    if not token:
        return None
    try:
        payload = decode_token(token)
        user_id = payload.get("sub")
        if user_id is None:
            return None
        result = await db.execute(select(User).where(User.id == user_id))
        return result.scalar_one_or_none()
    except (JWTError, ValueError):
        return None
```

- [ ] **Step 6: Commit**

```bash
git add backend/app/schemas/ backend/app/dependencies.py
git commit -m "feat: add pydantic schemas and auth dependencies"
```

---

*(Plan continues with remaining Sprint 1 tasks: auth service, SMS service, auth API, file upload/OSS, system config service, diagnose service, projects API, workspace API, frontend initialization, shadcn/ui setup, login/register/forgot-password pages, workspace/project list pages, integration testing.)*

---

**Note:** This plan covers Task 1-6 in complete detail (project init, Docker, FastAPI framework, database models, Alembic, security utils, schemas). Due to the large scope of Sprint 1, the remaining tasks (auth API endpoints, SMS mock, file upload, OSS, system config, diagnose, frontend setup, pages) follow the same TDD pattern and will be executed sequentially after these foundational tasks are complete and verified.
